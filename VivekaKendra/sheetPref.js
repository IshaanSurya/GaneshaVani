var g_InstOrgName="Vivekananda Kendra Vidyalaya, Kallubalu." ;
var g_dynSheetMoodImagePath="../../../images2/";
var g_renderingType=""; //L (legacy), T (templated), Generated (G)
var gv_FilterAns="GV_FA"

// Search and replace string in files.
// Note: (here [ is used as a boundery token. Any char after 's is considered as a boundary token)) sed -i 's[searchstring[replacestring[g
// Actual example (here ` is the boundary token) : sed -i 's`</tbody>`<div id="di_GVL"> <span id="Sp_GVL"></span></div></tbody>`/g' *.html

